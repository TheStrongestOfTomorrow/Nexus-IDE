name: Desktop Releases
on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - name: Rust setup
        uses: dtolnay/rust-toolchain@stable

      - name: Node.js setup
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build

      - name: Setup Valid Icons
        run: |
          mkdir -p src-tauri/icons
          curl -L https://raw.githubusercontent.com/tauri-apps/tauri/dev/tooling/cli/templates/app/icons/32x32.png -o src-tauri/icons/32x32.png
          curl -L https://raw.githubusercontent.com/tauri-apps/tauri/dev/tooling/cli/templates/app/icons/128x128.png -o src-tauri/icons/128x128.png
          curl -L https://raw.githubusercontent.com/tauri-apps/tauri/dev/tooling/cli/templates/app/icons/128x128@2x.png -o src-tauri/icons/128x128@2x.png
          curl -L https://raw.githubusercontent.com/tauri-apps/tauri/dev/tooling/cli/templates/app/icons/icon.icns -o src-tauri/icons/icon.icns
          curl -L https://raw.githubusercontent.com/tauri-apps/tauri/dev/tooling/cli/templates/app/icons/icon.ico -o src-tauri/icons/icon.ico

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name || 'latest' }}
          releaseName: "Nexus IDE ${{ github.ref_name || 'Latest Build' }}"
          releaseBody: "Automated build of Nexus IDE v4.3"
          releaseDraft: true
          prerelease: false
